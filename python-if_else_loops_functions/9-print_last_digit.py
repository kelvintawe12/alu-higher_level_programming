#!/usr/bin/python3
def print_last_digit(numbers):
    if number < 0:
        last_digit = number % -(10)
        print(-(las_digit), end='')
    else:
        last_digit = number % 10
        print(last_digit, end='')
    return abs(last_digit)
